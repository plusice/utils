/**
 *
 */
class TreeData {

  /**
   * @param  {object} treeData 构造树的数据
   * @param  {array} seleted  已选择的数据
   */
  constructor(treeData, seleted) {
    this.treeData = treeData;
    this.seleted = seleted;
  }

}